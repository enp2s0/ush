language: c

before_install:
  - export EXECUTABLE=bin/ush-debug
  - export STRIPBIN=bin/ush-run

script:
  - make clean
  - make all
  - make strip
  - bin/ush-run —help
  
after_success:
  - echo "Success!"
